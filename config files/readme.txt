Each server the bot is a part of will have an entry here with personalised settings.
When the bot joins a fresh server it will use the default configuration, when it is set up on that server the configuration 
will be saved to a file with the name of that servers ID